<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema-instance">
    <!-- Definición del tipo simple reutilizable -->
    <xs:simpleType name="tipoDeVehiculo">
        <xs:restriction base="xs:string">
            <xs:enumeration value="barco"/>
            <xs:enumeration value="bicicleta"/>
            <xs:enumeration value="coche"/>
            <xs:enumeration value="tren"/>
        </xs:restriction>
    </xs:simpleType>

    <!-- Definición del elemento "vehiculo" utilizando el tipo reutilizable -->
    <xs:element name="vehiculo" type="tipoDeVehiculo"/>
</xs:schema>